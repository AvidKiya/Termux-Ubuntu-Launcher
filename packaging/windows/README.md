# AvidKiya DevHub Windows EXE

Build on Windows PowerShell:

```powershell
cd avid-termux-ubuntu-launcher
.\packaging\windows\build_exe.ps1
```

Output:

```text
dist\AvidKiyaDevHub.exe
```

For VS Code, open the project folder and run the same PowerShell command in the terminal.
